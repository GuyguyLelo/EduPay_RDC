[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = ">=5.2,<6.0"
djangorestframework = ">=3.15.0,<4.0"
psycopg2-binary = "==2.9.9"
requests = ">=2.31.0"
python-decouple = ">=3.8"
gunicorn = ">=21.0.0"
whitenoise = ">=6.6.0"
pillow = ">=10.0.0"
cryptography = ">=42.0.0"

[dev-packages]

[requires]
python_version = "3.11"
